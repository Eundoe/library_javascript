const fs = require('fs');

/** 폴더 경로를 생성하는 함수 직접적으로 폴더를 생성하며
 * 존재할경우에는 통과 존재하지 않을경우에는 통과하지않음 파일경로른 "/" 를 기준으로 쪼개기
 * 이유는 리눅스를 이용하기 때문에 기본적으로 저기준으로 쪼개기 진행
@param {String} path 경로설정 "/" 기준으로함
@version 0.0.1 UPDATED 2025.08.04
*/
function generateFolderPath(path) {}

function generateJsonFile(value) {}
